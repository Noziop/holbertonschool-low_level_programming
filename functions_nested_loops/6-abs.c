#include "main.h"
#include "main.h"

/**
*_abs - print the absolute value of a num
*@z: value being tested.
*
*Description: print absolute value
*Return: int n
*/

int _abs(int z)
{
	if (z < 0)
	{
		return (-z);
	}
	else
	{
		return (z);
	}
}
