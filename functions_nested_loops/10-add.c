#include "main.h"
#include <stdio.h>

/**
 * add - entry point
 *@x: values to add to y.
 *@y: value to add to x
 *
 * Description: does an addition
 * Return: int to main.
 */

int add(int x, int y)
{
	return (x + y);
}
